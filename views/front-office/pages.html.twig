       {% extends "base.html.twig" %}

      {% block content %}
<section class="m-profile">
    <div class="container">
        {% for page in pages %}
        <h1>{{page.title}}</h1>
        <div class="row justify-content-center">
            <div class="col-lg-12">
               {{page.content}}
            </div>
        </div>
        {% endfor %}
    </div>
</section>
{% endblock %}